<%= render 'shared/description' %>

<%= render 'shared/years_first' %>

<% @items.each do |item| %>
  <%= render 'shared/graph', :item => item %>

  <%= render @items.last == item ? 'shared/years_last' : 'shared/years' %>
<% end %>

<% if @main.type == 'region' && @item.type == 'sector' %>

<%= render 'shared/decades_analysis' %>

<% end %>